{
  "routes": [
    {
      "method": "GET",
      "path": "/hero",
      "handler": "Hero.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/hero/:_id",
      "handler": "Hero.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/hero",
      "handler": "Hero.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/hero/:_id",
      "handler": "Hero.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/hero/:_id",
      "handler": "Hero.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
